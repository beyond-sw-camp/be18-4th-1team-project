name: Frontend CD

#on:
#  push:
#    branches:
#      - main

# 테스트용
on:
  pull_request:
    branches:
      - develop

jobs:
  deploy:
    runs-on: self-hosted
    name: "Deploy to Local Kubernetes"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check kubectl context
        run: |
          kubectl config current-context
          kubectl get nodes

      - name: Apply Kubernetes manifests
        working-directory: ./frontend/smerp-client/k8s
        run: |
          kubectl apply -f service.yaml
          kubectl apply -f deployment.yaml

      - name: Update Deployment with new image
        run: |
          IMAGE_TAG=${{ github.sha }}
          echo "Deploying image tag: $IMAGE_TAG"
          kubectl set image deployment/smerp-frontend \
            smerp-frontend=${{ secrets.FRONT_DOCKER_USERNAME }}/${{ secrets.FRONT_IMAGE_NAME }}:$IMAGE_TAG

      - name: Wait for rollout to complete
        run: |
          kubectl rollout status deployment/smerp-frontend --timeout=90s

      - name: Check running pods and services
        run: |
          kubectl get pods -o wide
          kubectl get svc -o wide
