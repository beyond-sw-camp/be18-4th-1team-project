apiVersion: apps/v1
kind: Deployment
metadata:
  name: smerp-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smerp-frontend
  template:
    metadata:
      labels:
        app: smerp-frontend
    spec:
      containers:
        - name: smerp-frontend
          image: promandu/smerp-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
